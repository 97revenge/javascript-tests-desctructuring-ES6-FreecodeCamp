const localForecast = {
  yesterday: { low: 61, high: 75 },
  today: { low: 64, high: 77 },
  tomorrow: { low: 68, high: 80 },
};

const { yesterday, today, tomorrow } = localForecast;

const {
  today: { low: lowToday, high: highToday },
} = localForecast;

console.log(today);







// const {yesterday: {low , high}) = localForecast;

// const{} = lowToday;
// const{} = HighToday;

// Altere apenas o código acima desta linha

// "eu nao gosto nenhum pouco de Upper-case entao troquei para CamelCase."

// Altere apenas o código abaixo desta linha

// BASE DE TESTES ANTIGOS :

// const { yersterday: lowToday, tomorrow: highToday } = localForecast;

// const { yesterday: { today.low }} = lowToday;
// const {tomorrow: {localForecast.today.high}} = highToday;

//  const lowToday = LOCAL_FORECAST.today.low;
//  const highToday = LOCAL_FORECAST.today.high;
